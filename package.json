{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Backend for phonebook application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "cd phonebook-frontend && npm install && npm run build",
    "deploy": "npm run build:ui && cp -r dist ../",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.17.1",
    "mongodb": "^6.9.0",
    "mongoose": "^8.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
